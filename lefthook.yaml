pre-commit:
  jobs:
    - name: Lint
      run: npx eslint {staged_files} --fix
      glob:
        - '*.ts'
      stage_fixed: true
    - name: Format
      run: npx prettier --write {staged_files}
      glob:
        - '*.ts'
      stage_fixed: true
